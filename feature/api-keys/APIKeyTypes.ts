type KeyPair = {
  id: string
  name: string
  expires_at_days: string
  createdAt: string
}

type CreatedKeys = {
  id: string
  name: string
  expires_at_days: string
  createdAt: string
  publicKey: string
  privateKey: string
}